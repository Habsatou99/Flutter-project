//import 'package:flutter/material.dart';
/*import 'package:project2/Models/JobeurModel.dart';
import 'package:project2/Models/ProfilModel.dart';
import 'package:project2/jobFeed.dart';
import 'package:provider/provider.dart';

import 'Models/JobModel.dart';
import 'jobeurFeed.dart';

class ProfilList extends StatelessWidget {
  ProfilList({Key? key});

  @override
  Widget build(BuildContext context) {
    final _profils = Provider.of<List<Profil>>(context);
    return SliverList(
        delegate: SliverChildBuilderDelegate((_, index) {
      return jobeurFeed(jobeur: _profils[index]);
    }, childCount: _jobeurs.length));
  }
}*/
